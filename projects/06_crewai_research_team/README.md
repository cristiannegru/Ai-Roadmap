# 06 — Autonomous Research Team (Expert)

Researcher → Writer → Critic over any docs folder. Offline demo.

```bash
cd projects/06_crewai_research_team
pip install -r requirements.txt
python main.py --query "What is the ReAct pattern?"
```

Writes a cited `outputs/brief.md`. Reuses `templates/crewai_team/crew.py`
so template improvements flow straight into this project.
