# ── Core runtime (lean: everything runnable offline) ───
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
scipy>=1.12
jupyter>=1.1
notebook>=7.2
tqdm>=4.66
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31
pillow>=10.3

# ── API + UI (Phase 7, Chunk 6) ────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
streamlit>=1.33

# ── DL (Chunk 3+: CPU-fast, no CUDA required) ──────────
# ~200MB. Apple Silicon uses MPS automatically via get_device().
torch>=2.3
# torchvision>=0.18  # only needed for real MNIST/vision datasets (Chunk 5)

# ── GenAI / RAG / Agents (Chunk 4, optional extras) ────
# langchain>=0.2
# langchain-community>=0.2
# llama-index>=0.10
# chromadb>=0.5
# sentence-transformers>=2.7
