-r requirements.txt

# ── Dev / CI tooling (pip-installable only) ────────────
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27
ruff>=0.4
black>=24.4
mypy>=1.10
pre-commit>=3.7

# NOTE: link/markdown linters are NOT pip packages — consumed via:
#   - links workflow: lycheeverse/lychee-action (links.yml)
#   - markdown job: npx markdownlint-cli (ci.yml)
